DROP TABLE IF EXISTS Patient
CREATE TABLE Patient (
    id INT PRIMARY KEY IDENTITY(1,1),
    oncId INT UNIQUE NOT NULL,
    DOB DATE NOT NULL,
    sex VARCHAR(1) NOT NULL,
    height FLOAT NOT NULL,
    weight FLOAT NOT NULL,
    BMI FLOAT,
    BSA FLOAT
);